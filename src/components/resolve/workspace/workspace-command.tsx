"use client";

import { WorkspaceOS } from "@/components/resolve/workspace/workspace-os";

/** Full-viewport command center — no marketing header, no card stacks. */
export function WorkspaceCommand() {
  return <WorkspaceOS />;
}
