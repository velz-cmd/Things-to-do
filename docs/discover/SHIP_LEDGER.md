# RESOLVE Discover — Ship Ledger

Truth-preservation ledger across sessions. Update this file whenever a
phase's status, a blocker's state, or a cross-tab regression changes.
Never delete an unresolved item — mark it resolved with evidence, or
leave it open.

Last updated: 2026-08-24 · HEAD `7dffae78`

## Phase status

| Phase | Status | Notes |
|---|---|---|
| 1 — Economic data foundation | PASS | Canonical market layer, source health, deterministic ordering |
| 2 — Software economic graph | PASS | Releases, dependency edges, FUNDING.yml, role truth, durable merge |
| 3 — Research economic graph | **BLOCKED** | Code-complete; `DiscoverResearchSnapshot` migration not yet applied to Supabase (external schema gate) |
| 4 — Media/Creator economic graph | **IN PROGRESS / PARTIALLY BLOCKED** — corrected 2026-08-24, was previously overstated as "content-complete" | See "Phase 4 truth correction" in the session log below for the full, non-persistence gap list this ledger previously omitted. |
| 5 — Funding and demand graph | **IN PROGRESS** (16 Release Slices shipped, not PASS) | Slices 8-10 closed the user's four named foundational gaps (real coverage loader, live policy fingerprint, real settlement state feeding the resolver). Slice 11 built a canonical action shadow resolver and proved 3 real safety gaps in the legacy (economic-state-blind) action system via a 10-row parity matrix. Slice 12 shipped the safe restriction-only action cutover + server-side stale-state revalidation on `/api/wallet/send`. Slice 13 closed a real class of silent-failure bugs across the coverage/policy/overlap layer (fail closed, never open). Slice 14 closed the obligation-identity gap without a schema migration - real obligationId computed at settlement time, persisted additively into existing JSON payloads, making exact-obligation duplicate detection genuinely engage for the first time. Slice 15 added real database-level atomic duplicate-payment protection (partial unique index on `ActionRun.obligationId`, application blocked only on this sandbox's missing `DATABASE_URL` - same external blocker as Phase 3) plus a full cross-endpoint money-mutation audit finding every other Discover-reachable money endpoint already safe. **Slice 16 shipped the Details UI Funding projection** - `EconomicActionItem.economicState` now flows through to a real compact Funding section in the Details drawer, but required three real fix rounds after live screenshot verification kept finding it empty: the true root cause (found via a raw RSC-payload inspection, not guessed) was that `data.economicActions` is a VIEW-FILTERED feed that can omit a real item entirely, and `findContext()` needed to build a genuine `EconomicActionItem` via the same `actionFromOpportunity()` the server already uses, not just patch a missing field on an already-found match. Final state screenshot-confirmed live on Preview. See the Slice 8-16 session-log entries and the "Phase 5 acceptance-matrix self-check" entry for the full honest gate-by-gate status. **Genuinely still open**: obligation identity itself remains a computed provisional value for the non-policy dimensions; the atomic uniqueness constraint is written and checked in but not yet applied to a live database (external blocker); a full cross-endpoint money-mutation audit exists for the endpoints checked in Slice 15 but was not exhaustive across the entire RESOLVE platform (deliberately scoped to Discover-reachable endpoints). |
| 6 — Verified Work production surface | IN PROGRESS | Live-audited real Preview (desktop + mobile) first. Found and fixed a significant real gap: server-side pagination existed but was never surfaced in the UI - 30 of 47 real verified-work outcomes were completely unreachable. Live-verified the fix end to end on Preview post-deploy. Composition (17→47 real, all software/GitHub, zero research/media, "impact not yet measured" everywhere) is accurate given the already-documented Phase 2/3/4 external blockers, not a fresh defect. |
| 7–37 | Not started / in progress | See individual phase reports as they land |

## Global blockers (carry forward every session — do not drop)

- **Authenticated Playwright**: PENDING. No `E2E_*`/Supabase test secrets configured in this repo's CI. Never report PASS from a skipped run.
- **Phase 2 live external-enrichment proof**: PENDING. Libraries.io/npm/Docker Preview-env keys appear unconfigured; code path is real and tested, but live-data proof against real numbers has not been captured.
- **Phase 3 research schema migration**: BLOCKED. `prisma/migrations/20260823112411_discover_research_snapshot/migration.sql` is checked in and additive-only. This session's environment cannot reach Supabase over the Postgres wire protocol (HTTPS egress works; raw Postgres ports do not complete a handshake). Apply via the Supabase SQL Editor/dashboard, then resume Phase 3 (ingest → verify → clean-user → screenshots → PASS gate).
- **Agent payment availability**: CORRECTED 2026-08-23 — verified live on Preview. `isLiveArcEnabled()` (`src/lib/settlement/arc-config.ts:55`) checks the ERC-8183 feature flag + real Circle credentials + zero live blockers; all 6 real Agent Marketplace services render without the "Payment paused" badge (`discover-marketplace.tsx:1180-1184`), confirmed via authenticated screenshot with no transaction triggered. Arc Testnet USDC service payments are actually live-enabled on this deployment right now.
- **Public Pool market**: verified via live Preview screenshot — deterministic auto-listing (`computePoolListingEligibility`) already puts public market-listed Pools first, operator backlog second. No fix was needed.
- **Public Request market**: verified via live Preview screenshot — public funded Requests render independent of viewer.
- **Verified Work composition**: verified via live Preview screenshot (authenticated) — 12 real outcomes, currently all software/GitHub (navidrome releases) rows. This is expected, not a bug: research rows can't populate until the Phase 3 Supabase migration is applied, and media rows don't exist until Phase 4 ships. Re-check composition after each of those lands.
- **Activity tab public/private gating**: FIXED and shipped (commit `13663aeb`) — confirmed outcomes now render unconditionally, personal ledger sign-in-gated separately. Static regression guard in `tests/unit/discover-activity-shared-market.test.ts`.
- **Media shared market**: IN PROGRESS / PARTIALLY BLOCKED — see "Phase 4 truth correction" in the session log for the full code-slices-completed vs. open-non-schema-gaps breakdown. Not accurately described as blocked only on Postgres; real product-semantic gaps remain open independent of the schema gate (aggregation, identity-claim, period/obligation model, multi-source/multi-operator extensibility, clean-user proof, screenshot QA, accessibility).

## Cross-tab regression checklist (run after every phase)

- [x] Verified Work loads, shows shared multi-domain inventory (software confirmed live; media code-complete but not yet populated in this account's ListenBrainz feed; research pending the Supabase gate), zero console errors
- [x] Open Requests loads, public funded Requests visible independent of viewer
- [x] Pools loads, market-listed Pools visible independent of viewer, no bureaucracy-first UX
- [x] Agent Marketplace loads, real service list, live payments confirmed enabled, no dead buttons
- [x] Activity loads, shows real market events (confirmed outcomes), not raw account history — fixed this session

Re-run after commit `b54cfb6d` (Phase 5 media-matching fix): all five tabs still 200/0 console errors.

**Correction, 2026-08-24**: the smoke script driving every "5-tab" claim above through commit `a4949593` used the wrong query parameter (`?tab=` instead of `?view=`) and was actually re-testing the same default view five times per run, not the five distinct tabs. See the "Verification tooling correction" entry in the session log below for the full explanation. The re-run at commit `1f224505` is the first one this ledger can attest as genuine five-distinct-tab coverage.

## Session log

### 2026-08-23 — Cross-tab public-market correction session
- Verified repo truth: branch `fix/discover-hydration-and-phases`, HEAD `b88863a2`, clean working tree, no divergence from `origin/main` beyond the known merged-PR pattern.
- Created this ledger.
- Audited Pools/Requests: already correct, no code change needed.
- Found and fixed a real bug: Activity tab's `OutcomesView` early-returned on `!data.signedIn`, hiding genuinely public confirmed settlements (`loadConfirmedOutcomes()`, viewer-independent) from anonymous/clean visitors. Restructured to render the public section unconditionally, personal ledger sign-in-gated separately. Added static regression guard. Also root-caused and fixed a real CI timing flake (`github-public-analyze-persistence.test.ts` first-dynamic-import cost exceeding default 5s timeout on `pull_request` runs). Both shipped: commit → push → CI green → Vercel Preview exact-SHA verified → 5-tab smoke PASS. Final HEAD `33466a74`.
- Verified Agent Marketplace payment truth: `isLiveArcEnabled()` is genuinely true on this Preview (real Circle credentials + ERC-8183 flag + no live blockers) — all 6 services show live CTAs, no "Payment paused" badge. Corrected the ledger's prior stale claim that payments were not live-enabled. No transaction was triggered during verification.
- Verified Verified Work composition: 12 real software/GitHub outcomes, no research/media rows yet — expected given Phase 3 is schema-blocked and Phase 4 hasn't started.
- Started Phase 4 (Media/Creator Economic Graph). Audited existing code first per this session's standing discipline: `loadMediaSignals()` (ListenBrainz playback signals) and its wiring in `query.ts` already existed from an earlier session, already correct on identity (strong MusicBrainz recording ID preferred, `canonicalMediaId()`), semantics (never infers royalty/ownership/popularity from a play), UI labeling (no GitHub-styled leak — "Media"/"ListenBrainz" labels), and economic matching (real Pool `creator`-class eligibility, no fabricated per-play rate). `musicbrainz-oauth.ts`/`jellyfin-*.ts`/`navidrome*.ts` turned out to be the user's own Navidrome→ListenBrainz scrobble-pipeline account-linking utilities, not additional independent Discover market data sources — no gap there. The one real, previously-undiagnosed gap: `loadMediaSignals()` swallowed every ListenBrainz failure silently with zero surfaced health, unlike GitHub's connector diagnostics. Added `loadMediaSourceDiagnostic()` (reuses the existing real `pingListenBrainz()` network check, reports connected/refresh_failed honestly, never fabricates `lastSuccessfulAt` on failure) and wired it into the same `sourceDiagnostics` list. Shipped: typecheck clean, 671/671 tests passing (7 new), CI green, Vercel Preview exact-SHA verified (`487dacb`), 5-tab smoke PASS. Final HEAD `487dacb5`.
- Closed a second real Phase 4 UI gap: the generic `WorkRow` only special-cased `research_work` and `open_collective_contribution` — `listenbrainz_listen` items fell through to the GitHub-shaped default (GitBranch icon, GitHub payout/coverage copy), the exact class of styling leak this session has been correcting elsewhere. Added a dedicated `MediaWorkRow` (no GitBranch icon, no fabricated funding CTA — `attachVerifiedWorkActions()` never touches non-`github_evidence` sources, so the real "View on ListenBrainz" primary action is used as-is) plus a static regression guard (`discover-media-work-row.test.ts`). Shipped: typecheck clean, 673/673 tests passing, CI green, Vercel Preview exact-SHA verified (`0a13e88`), 5-tab smoke PASS. Final HEAD `0a13e88c`.
- Closed the last non-Postgres Phase 4 gap: deterministic media identity uncertainty detection (`mediaIdentityUncertainty()`), mirroring research's Part E model - pure function, no network calls. Flags when a listen's identity rests on the weak artist/track/timestamp fallback instead of a real MusicBrainz recording ID (two different recordings can share a title). Unlike research's Crossref/OpenAlex citation-count comparison, a discrepancy-between-sources category does not exist for media yet since ListenBrainz is the only provider - not fabricated. Surfaced through the existing `riskFlags` field (previously always `[]`, so populating it alone would have been invisible) and rendered in `MediaWorkRow`'s Details drawer. Shipped: typecheck clean, 676/676 tests passing (4 new), CI green, Vercel Preview exact-SHA verified (`39e6a2a`), 5-tab smoke PASS. Final HEAD `39e6a2a2`.
- Phase 4 is now content-complete except for durable Postgres persistence with source-aware merge, which is the same external Supabase-reachability gate already blocking Phase 3 - both phases are now explicitly BLOCKED on that one external item, not on missing work in this sandbox.

### 2026-08-24 — Phase 4 truth correction
The previous entry above ("content-complete except for durable Postgres persistence") was an overstatement and is corrected here rather than silently edited away, per this ledger's own "never erase an unresolved item" rule.

**CODE SLICES ACTUALLY COMPLETED (real, shipped, verified):**
- Real ListenBrainz playback observations (`loadMediaSignals()`, pre-existing)
- `canonicalMediaId()` strong-MBID-over-weak-fallback identity preference (pre-existing)
- Playback-only semantics (never infers royalty/ownership/popularity from a play)
- Real per-provider source health (`loadMediaSourceDiagnostic()`, commit `487dacb5`)
- Dedicated `MediaWorkRow`, no GitHub visual leak (commit `0a13e88c`)
- Deterministic weak-identity uncertainty detection (`mediaIdentityUncertainty()`, commit `39e6a2a2`)
- Media now genuinely reaches the economic matcher (commit `b54cfb6d`, Phase 5)

**EXTERNAL SCHEMA DEPENDENCY:**
- Durable Postgres persistence with source-aware merge (mirroring Phase 3's `DiscoverResearchSnapshot` pattern) — blocked on the same Supabase wire-protocol unreachability as Phase 3.

**OPEN NON-SCHEMA PRODUCT GAPS (not started, not fixable by the schema landing):**
- Shared/public media source model — today's single-account ListenBrainz feed has no defined multi-user/multi-operator model.
- Private listening-history non-leakage proof — no test or audit has confirmed a private listening account's data cannot leak into another viewer's projection once multiple accounts exist.
- Canonical recording-level aggregation instead of raw-listen market spam — each listen is currently its own market row; 10 plays of the same track today would be 10 separate rows, not one recording-level outcome with an observation count. Not yet addressed.
- Observation scope semantics (per-listen vs per-recording vs per-artist) — undefined beyond the single-listen row.
- Period semantics for creator obligations (one-time vs monthly vs policy window) — does not exist yet; ties into Phase 5's period model.
- Multi-source aggregation (MusicBrainz/Last.fm cross-validation the way research cross-validates Crossref/OpenAlex) — ListenBrainz is still the only real data source; Last.fm exists only as a ping/cross-validation stub (`pingLastFm()`), not a market data source.
- Multi-operator/community extensibility — the pipeline is hardcoded to one configured `LISTENBRAINZ_USERNAME`, not a per-community or per-operator model.
- Verified creator identity vs payout identity — media items never establish "this artist is a claimed RESOLVE identity," only that a play was observed; no identity-claim flow exists for creators the way GitHub contributor claiming does.
- Explicit creator Pool eligibility beyond broad class matching — today a Pool matches on the coarse `"creator"` class only (see Phase 5 section below); there is no scoping to a *specific* creator/recording the way a Request targets a specific beneficiary.
- Creator obligation foundation — no `Obligation` entity exists for media (or any domain) yet; this is Phase 5 scope, not yet built.
- Coverage-by-period semantics — not applicable yet since no obligation/period model exists.
- Source navigation ("View on ListenBrainz") is still the only real primary action; it is appropriately not a fabricated economic CTA, but this also means no real economic CTA exists yet for media — correct given no Obligation model exists, but worth stating plainly rather than implying completeness.
- Clean-user shared-media proof — not yet captured (requires either multiple real accounts or the schema/persistence work to observe more than one account's data).
- Live persisted media acceptance — blocked on the schema gate above.
- Desktop/mobile screenshot QA with real, populated public media inventory — not yet captured; existing screenshots show zero or a handful of items from a single account, not a populated multi-item market.
- Accessibility audit specific to `MediaWorkRow` — not yet performed (general Details/`<details>` pattern is shared with `ResearchWorkRow`/`WorkRow`, but no dedicated pass has run against the media row specifically).

None of the above are magically resolved by applying the Supabase migration. The schema gate blocks persistence and live multi-item acceptance; it does not touch aggregation semantics, identity-claim flows, period/obligation modeling, or multi-source/multi-operator extensibility, all of which remain genuinely open Phase 4 (and, where they overlap with the general obligation model, Phase 5) work.

### 2026-08-23/24 — Phase 5 (Funding and Demand Graph) started
- Audited the existing economic-matching architecture directly (not via a dead-end fork retrieval attempt - noted for future sessions: forking to "retrieve a prior fork's report" does not work as expected, since a resumed fork only has its own transcript, not a synthesized report; read the source directly instead):
  - `economic-matching.ts` (294 lines): a real, deterministic, already well-built matcher - `FundingIntentCandidate`/`CoverageRecord`/`EconomicMatch` types, `assessOverlap()` already separates purpose from beneficiary (a prior payment for a different purpose never blocks a new one; the exact same obligation is never repaid), `matchImpactToCapital()` already gates delegated capital (Pool/Program/Request) on real sourced impact evidence while leaving direct/recurring support ungated. This module already substantially satisfies Phase 5's Demand/Eligibility/Coverage/Overlap requirements for the mechanisms it covers.
  - `attach-economic-match.ts` (153 lines): bridges the matcher to the live marketplace. **Found and fixed a real gap**: the type guard only allowed `github_evidence` and `research_work` through - `listenbrainz_listen` (media) was silently excluded from ever receiving `economicMatch`, even though `poolOutcomeClasses()`/`outcomeClassFor()` already handle the `"creator"` class correctly. A real Creator Pool could exist and still never match a verified play. This directly corrects an inaccurate claim in this ledger from earlier in the session ("economic matching already correct" for media - it was not; the match was unreachable, not present). Fixed by extending the guard; shipped as commit `b54cfb6d`.
  - `funding-coverage.ts` (621 lines): a separate, richer, GitHub-specific "Command Centre" ledger state model (`FundingAmountState`, `FundingSettlementState`, `ContributorReadiness`) with its own state enum, parallel to `EconomicMatch` - confirms the spec's diagnosis that state is genuinely scattered across two systems today. Not touched this slice; a full unification is a larger, separate piece of Phase 5 work, not yet started.
  - `economic-actions.ts` (428 lines): builds a global cross-domain action feed (`buildEconomicActions()`, consumed by `query.ts` for the "for you"/recommendation queue, not row rendering). Checked whether media items get a degraded action here the way the `WorkRow` bug did - `isWork()` doesn't recognize `creator_collaboration` (media's type), so media items fall through to a generic "Inspect economic state" `detailAction` in this specific feed. **Investigated and decided NOT a bug**: `research_work`/`"research_outcome"` go through the identical fallback and this was never flagged as broken during Phase 3's acceptance - it is consistent, deliberate behavior for every non-classic-"work" domain in this specific global feed, not a media-specific regression. No fix made; documented here so a future session doesn't waste time re-diagnosing it.
  - `valuation-eligibility.ts`/`funding-preflight.ts`: `valuation-eligibility.ts`'s block-based per-listen/per-star estimates ("$10 per qualifying block") looked, at first glance, exactly like the "never manufacture money from stars/listens" violation Phase 5 explicitly forbids - but its import graph (`opportunity-score.ts`, `trending-gaps.ts`, `capital/funder-discovery.ts`, `capital/community-yield.ts`) traces to the separate Capital/Earn/Communities product surfaces, never to the Discover marketplace pipeline (`query.ts`/`discover-marketplace.tsx`). Confirmed out of scope for Discover Phase 5 - not touched. `funding-preflight.ts` (real Arc/Circle preflight checks: balance, spending limit, idempotency key, authorization) is real and already correct; no gap found.
- Shipped: 4 new negative/positive matching tests for media (mirrors the existing research pattern - no funding intent → no match; real eligible Creator Pool → reachable match; wrong-class Pool → no match; playback alone never grants direct-support eligibility), 680/680 tests passing, typecheck clean, production build green, CI green, Vercel Preview exact-SHA verified (`b54cfb6`), 5-tab smoke PASS. Final HEAD `b54cfb6d`.
- Next: continue Phase 5 - the canonical single-state-enum unification (spec section 6) that would fold `funding-coverage.ts`'s separate Command Centre model and `economic-matching.ts`'s `EconomicMatch` into one resolver is the largest remaining real gap, plus partial-coverage amount tracking (spec section 10, currently binary has-coverage/no-coverage, not amount-delta based) and policy versioning (spec section 13, currently absent entirely). Continue per the standing "do not stop" directive; keep Phase 3 BLOCKED and Phase 4 IN PROGRESS/PARTIALLY BLOCKED (not "content-complete").

### 2026-08-24 — Phase 4 ledger truth correction, then Phase 5 Release Slices 1-2
- Corrected the prior "Phase 4 content-complete except persistence" overstatement per direct user instruction - the original claim is preserved above and explicitly corrected, not silently edited away, per this ledger's own rule. Real non-schema Phase 4 gaps now documented: recording-level aggregation (currently one row per listen, not per recording), creator identity-claim flow, period/obligation model, multi-source/multi-operator extensibility, clean-user proof, screenshot QA, accessibility. None of these are resolved by the Supabase migration landing.
- **Release Slice 1** (canonical economic state model): added `src/lib/discover/marketplace/economic-state.ts` - one `CanonicalEconomicState` projection consumed from both `EconomicMatch` (`resolveCanonicalEconomicStateFromMatch()`) and funding-coverage.ts's GitHub ledger record (`resolveCanonicalEconomicStateFromLedgerRecord()` - funding-coverage.ts becomes a domain adapter, not a competing state machine). Amount-based coverage (confirmed vs. pending vs. remaining, overfunding floored at zero via `computeCanonicalCoverage()`). `CoverageRecord` gained an optional `status` field (additive, defaults to confirmed - zero behavior change for every existing caller). Documented precedence order: settlement-in-flight > duplicate-obligation > full coverage > unresolved review requirement (outranks partial coverage - an ambiguous same-purpose prior payment must block further funding, not just get labeled "partial") > partial coverage > payout readiness > ready-to-fund. Caught and fixed a real precedence bug during test-writing (`requiresReview` was checked after coverage amounts, letting partial coverage silently bypass an unresolved review flag). 28 tests. Shipped: 704/704 total tests passing, typecheck clean, CI green, Vercel Preview exact-SHA verified (`a494959`), 5-tab smoke PASS. Final HEAD `a4949593`.
- **Release Slice 2** (policy fingerprint, stable obligation identity, period semantics, coverage-by-obligation): audited for an existing system before building anything new, per this session's standing discipline - found a real, already-Prisma-persisted `ProgramVersion`/`PolicyVersion` (with `contentHash`, unique on `[programVersionId, version]`) / `Obligation` (`lineageHash` unique) system in `prisma/schema.prisma`, wired through `src/lib/obligations/normalize-community.ts` and consumed by `/api/communities/[slug]/obligations`, `/api/settlement/batch`. Confirmed Discover's own Pools are the same underlying `ResolveProgram` rows (`loadProgramOpportunities()` in `query.ts` queries `prisma.resolveProgram`), so that system is genuinely applicable here in principle - but `ensureProgramPolicyVersion()` writes `ProgramVersion`/`PolicyVersion` rows transactionally as a side effect, which is the wrong shape for a marketplace READ path (a page view should not silently create database rows). Deliberately NOT wired into it this slice - that is a larger, separate piece of work requiring careful design of a read-only bridge, not an appropriate blind extension under "do not stop." Instead added pure, deterministic functions to `economic-state.ts`: `computePolicyFingerprint()` (order-independent - `eligibleClasses` sorted before hashing, since array order there is not semantically meaningful), `periodKey()`/`CanonicalPeriod` (one_time/calendar_month/policy_window), `computeObligationId()` (mechanism + canonical subject + purpose + period + beneficiary + policy fingerprint, case/whitespace-normalized purpose so formatting never forks identity), `filterCoverageByObligation()` (excludes untagged records rather than assuming they match - never sums a beneficiary's whole payment history into one obligation's coverage). 20 new tests covering determinism, order-independence, and every "different X produces different identity" case from the spec's own obligation-identity test list. Shipped: 24 new tests (28 file total for the slice), CI green, Vercel Preview exact-SHA verified (`1f22450`). Final HEAD `1f224505`.
- **Verification tooling correction (important, applies retroactively to every prior "5-tab smoke PASS" claim this session)**: while verifying Slice 2, `scratchpad/smoke-discover.mjs` started failing intermittently on Playwright's `networkidle` wait. Investigating turned up a real, separate defect in the verification script itself, not the app: the script has used `?tab=${tab}` as its query parameter for every smoke run this entire session, but the Discover page actually reads `params.view` (`parseDiscoverView(params.view)` in `src/app/(shell)/discover/page.tsx`), which falls back to the default `"for_you"` view when `view` is absent. `tab` is not a recognized param at all. This means every prior "5-tab smoke PASS" logged in this ledger and in commit messages this session (`33466a74`, `487dacb5`, `0a13e88c`, `39e6a2a2`, `b54cfb6d`, `a4949593`) actually re-tested the same default view five times, not the five distinct tabs it claimed to cover. **No defect was hidden by this** - each of those checks did get a genuine 200-status, 0-console-error response from a real page render - but the claimed breadth of coverage was wrong, and this is recorded here rather than silently fixed and left undocumented. Corrected the script to use `?view=${tab}` and switched its wait condition from `networkidle` (which was timing out, apparently due to some persistent background network activity on the page) to `domcontentloaded` with an explicit 25s timeout plus a 2.5s settle delay - the same pattern already used elsewhere in this session's screenshot tooling for the same `networkidle` fragility. Re-ran the corrected script against Slice 2's Preview deployment: all five real tabs (`verified_work`, `requests`, `pools`, `agents`, `activity`) return 200 with 0 console errors. Going forward, every "5-tab smoke PASS" in this ledger from this point on reflects the corrected script and is genuine per-tab coverage.
- **Release Slice 3** (wire canonical state into a real Discover projection): `MarketplaceOpportunity` gained an `economicState` field; `attachEconomicMatch()` computes it for every domain it already processes (software/research/media) via a new `payoutFromFinancialReadiness()` mapper. `MediaWorkRow` became the first real UI consumer, reading its collapsed-row funding line from the canonical projection instead of leaving it empty. 34 new tests, typecheck clean, local (cached) production build reported success, CI passed for the exact commit - **but the actual Vercel deployment failed** (see the correction entry immediately below). Do not trust a stale "shipped" claim from mid-verification; the real outcome is recorded in the next entry.
- **Real deployment failure and root cause (commit `7e2ecf77`)**: `discover-marketplace.tsx` (a client component) value-imported `canonicalStateLabel` from `economic-state.ts`, which has a top-level `import { createHash } from "node:crypto"`. A value import of anything from that module pulls its entire runtime module graph into whichever bundle imports it - breaking the browser bundle: `UnhandledSchemeError: Reading from "node:crypto" is not handled by plugins`. **Two compounding process failures let this ship without being caught immediately:**
  1. My local `npm run build` reported success for commit `7e2ecf77` because Next's incremental `.next` cache masked the bundle-graph error - it had already compiled `discover-marketplace.tsx` successfully in a prior run before `economic-state.ts` gained the `canonicalStateLabel` export, and the cache did not force a fresh cross-bundle check. A genuinely clean build (`rm -rf .next && npm run build`) reproduces the failure correctly. **Going forward, any commit that changes what a client component imports gets a clean (`rm -rf .next`) build check, not just a cached one** - this is now a standing practice for this session, not only a one-time fix.
  2. My Vercel-status polling loop filtered on `.context | contains("Vercel")`, which matched the wrong check - this repo now has two Vercel projects reporting on the same commits: `Vercel – things-to-do` (an unrelated project, coincidentally reporting `success`/"Canceled by Ignored Build Step" quickly) and `Vercel – resolve` (the actual RESOLVE app, which had genuinely failed). The generic substring match picked the first one alphabetically/chronologically and reported false-positive success. **Going forward, Vercel status checks in this session filter specifically on `context == "Vercel – resolve"`, never a generic `contains("Vercel")` substring match.**
  Fixed by splitting the crypto-free `canonicalStateLabel()` into `src/lib/discover/marketplace/economic-state-labels.ts`, whose only dependency is a type-only import of `CanonicalEconomicState` (type-only imports are erased at compile time and never pull in the source module's runtime graph). `economic-state.ts` keeps every crypto-using/server-only export. Verified via a genuinely clean build this time - the `/discover` route compiles successfully (45.7 kB route, 837 kB First Load JS). Same commit also completes **Release Slice 4** (funding-state label canonicalization): `fundingStateLabel()`/`researchFundingStateLabel()` now read the canonical projection first, closing the same duplication already closed for media. Shipped: CI green, `Vercel – resolve` context specifically verified success (not the unrelated `Vercel – things-to-do` context), Preview exact-SHA verified (`0234476`), 5-tab smoke PASS on retry (first attempt hit a transient `domcontentloaded` timeout consistent with this session's known Preview-cold-start flakiness, not a regression - retry succeeded cleanly with 0 console errors on all five tabs). Final HEAD `02344768`.
- Both Release Slice 3 and Release Slice 4 are now genuinely, verifiably shipped and live on Preview.
- **Release Slice 5** (reconciliation and negative financial states): added `detectReconciliationIssue()` - a pure, deterministic function comparing real settlement facts (chain confirmation, receipt existence, expected vs. confirmed amount, expected vs. confirmed recipient, duplicate-submission flag), checked in a fixed severity order (duplicate submission first, then presence mismatches, then amount, then recipient), returning the first real issue found. Wired into `CanonicalEconomicState` via a new optional `reconciliation` field, populated only when state is genuinely `reconciliation_required` (never speculatively). `canonicalStateLabel()` surfaces the specific real reason instead of a bare "Payment needs reconciliation". 12 new tests including the full negative matrix (consistent settlement -> no issue; receipt without chain confirmation -> `transaction_missing`, never assumed confirmed; a genuinely pending settlement with both sides absent -> no issue, since absence on both sides is consistent, not a discrepancy). Shipped: CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`6b6e6b0`), 5-tab smoke PASS on retry (first attempt hit a transient `ERR_NAME_NOT_RESOLVED` DNS blip, not a regression - retry clean). Final HEAD `6b6e6b01`.
- **Local test-flake investigated and root-caused, not dismissed**: while running the full suite for Slice 6, `github-public-analyze-persistence.test.ts` failed 3/5 tests with a real 502 `GITHUB_UNAVAILABLE` response instead of the expected mocked 200/503. Confirmed via `git stash` that this reproduces identically on the last clean committed HEAD (unrelated to any Slice 6 change), then root-caused via a temporary debug script: the route makes a real, un-mocked network call to `githubFetchOrThrow` (a GitHub repository-existence check) before ever reaching the mocked `ingestRepository()` - any transient failure of that real network call surfaces as the generic `GITHUB_UNAVAILABLE` catch-all. Ruled out GitHub anonymous rate limiting (44/60 remaining, checked directly) and repo deletion (confirmed 200 via plain curl) as causes - this was a genuine transient network blip in this specific sandbox run. Re-ran twice more: both times all 5 tests passed cleanly. Not caused by any commit in this session; CI (GitHub's own runners, a different network path) has passed independently throughout. No code change made - correctly investigated rather than either blindly dismissed as "flaky" or blindly "fixed" with an unnecessary retry/mock change.
- **Release Slice 6** (real funded Request/Campaign demand): audit turned up a real gap parallel to the earlier media-matching bug - `"funded_request"` was already a real `FundingMechanism` value in `economic-matching.ts` (already the HIGHEST-priority mechanism, ahead of Pool/direct support), but nothing in this codebase ever actually constructed one. A funded, public Campaign/Request could exist and still never match a real outcome - the mechanism was unreachable, not absent. Added `fundingIntentsFromCampaigns()` (mirroring `fundingIntentsFromPools()` exactly: remaining budget is the Campaign's own goal minus already-committed funding, never re-derived from anything else; an expired or fully-committed Campaign is still included, excluded with its real reason, matching the existing Pool "show why, don't hide" pattern), wired into `attachEconomicMatch()`. 8 new tests, including a real end-to-end match proving `funded_request` correctly outranks Pool/direct-support as the recommended mechanism when both are eligible. Shipped: 751/751 total tests passing, typecheck clean, genuinely clean production build, CI green, Vercel - resolve context specifically verified, Preview exact-SHA verified (`276f4d1`), 5-tab smoke PASS. Final HEAD `276f4d15`.
- **Release Slice 7** (purpose semantics use each work's real title): `attachEconomicMatch()` passed a single hardcoded generic string ("verified outcome support") as every outcome's purpose - Slice 6's real Campaign wiring made this concretely visible (a Campaign's own objective was being discarded for a placeholder). Now uses `item.title` - real, specific, already present on every item. 1 new test proving the real title actually flows through overlap comparison (a prior payment recorded against the same title produces a genuine `possible_overlap`, not a placeholder-based one). Shipped: 752/752 tests passing, genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`077ad63`), 5-tab smoke PASS. Final HEAD `077ad63e`.

### Phase 5 closing assessment - 7 Release Slices shipped, genuinely open items documented rather than force-completed
While scoping further work, investigated whether real settlement/receipt data could be wired into `coverageBySourceId` (currently: **never populated** in `query.ts`'s actual call to `attachEconomicMatch()` - `coverage` is empty for every live work item in production today, meaning the fully-built, fully-tested amount-based coverage model from Slice 1 has zero real data flowing into it in the live app). Traced the real live-settlement source (`buildLiveSettlements()`/`loadConfirmedOutcomes()`, `src/lib/discover/live-settlements.ts`) and found it is community/receipt-shaped (`LiveSettlementRow`: `communitySlug`, `receiptHref`, generic `kind: "fund"|"authorization"|"settlement"`), not clearly keyed to a specific `github_evidence`/`research_work`/`listenbrainz_listen` work-item source id - wiring it into `coverageBySourceId` would require further tracing (does `payload`/`sourcedHook` carry a per-work reference?) that was not completed this session.

Decision: rather than force this wiring, or the two other still-open items (obligation identity/period/policy fingerprint never called from the live path; UI primary-action button resolution not migrated from `attachVerifiedWorkActions` to `economicState.nextAction`, which the spec itself flags as needing incremental, careful migration given real financial-action risk) into an already very long session, all three are recorded here as genuinely open rather than silently dropped or force-completed. Phase 5 is **NOT** being marked PASS. Seven real Release Slices shipped this session, each fixing or building something concretely real (media-matching gap, canonical state model, funding-coverage domain adapter, policy fingerprint/obligation identity primitives, canonical projection wired into a live UI row, reconciliation model, real Campaign demand, real purpose semantics) - substantial, verified progress, honestly short of full acceptance.
Next: continue into Phase 6 (Verified Work Production Surface) per the master spec, while these three Phase 5 items remain open and tracked here for future continuation.

### 2026-08-24 — Phase 6 (Verified Work Production Surface) started
- Live audit first (screenshots at desktop 1440px and mobile 390px against the real Preview): 17 real software/GitHub outcomes, zero research/media rows - expected given Phase 3/4's external schema gate, not a fresh defect. Every row showed "Impact not yet measured" - also expected and already tracked (Phase 2 live external-enrichment proof PENDING, Libraries.io/npm keys unconfigured in Preview), not a fresh defect.
- **Found and fixed a real, significant gap**: the server already computed real cursor-based pagination (`MarketplacePage.nextCursor`/`total`, already served by the existing `/api/discover/opportunities` route) but it was never surfaced in the UI - `ForYouView` (the component actually backing the `verified_work` route) rendered only the first page with no "Load more" affordance at all. **Live verification revealed this was hiding real data at meaningful scale**: the database holds 47 real verified-work outcomes, but only 17 were ever reachable in the UI before this fix - 30 real outcomes were completely inaccessible. Added a real "Load more" button (shown only when a real next page exists, passes the active search query through, surfaces real fetch failures rather than swallowing them) that fetches and appends real pages via the existing API route. 4 new static regression tests. Live-verified end to end post-deploy: clicked the real button on the real Preview, confirmed the row count went from 18 to 31 DOM nodes and the label from "Load more (17 of 47)" to "Load more (30 of 47)", zero console errors. Shipped: 756/756 tests passing, genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`660b498`), 5-tab smoke PASS. Final HEAD `660b4982`.
- **A real, recurring test flake properly fixed (not dismissed a third time)**: `github-public-analyze-persistence.test.ts` failed again - this time on a `pull_request`-triggered CI run on GitHub's own runners (commit `b0b6e015`, a docs-only ledger commit, ruling out any code cause), with the identical 502 `GITHUB_UNAVAILABLE` signature investigated and provisionally excused as a local sandbox network blip earlier in this session. Recurring on CI's own infrastructure means it was never a sandbox-only issue - it is a genuine test-design defect: the route independently calls a real, un-mocked network function (`githubFetchOrThrow`, a GitHub repository-existence check) before ever reaching the already-mocked `ingestRepository()`. Fixed properly this time by mocking `githubFetchOrThrow` alongside the file's existing mocks - the test file's dependencies are now fully deterministic, no real network call remains anywhere in this test file. Verified: all 5 tests pass, and noticeably faster (no real network round-trip). Shipped: genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`b579735`), 5-tab smoke PASS. Final HEAD `b579735b`.

### 2026-08-24 — Phase 5 resumed per explicit user instruction: Release Slices 8-9 (real coverage + policy provenance)
User instructed stopping further Phase 6 expansion until four specific foundational Phase 5 gaps close: real settlement coverage flowing into `coverageBySourceId`, obligation identity/period/policy fingerprint made live (not just pure primitives), canonical `economicState.nextAction` owning the real primary action, and real settlement state feeding the canonical resolver.

- **Release Slice 8** (real confirmed/pending coverage loader wired into the live matcher): traced the real work-reward payment persistence path from repository truth before writing any code - `recordConfirmedDirectSupport()` (`src/lib/discover/direct-support.ts`) writes `SettlementBatch`/`ChainTransaction`(confirmed)/`Receipt` (payload includes `work: {subjectId, title, repository, sourceUrl, evidenceIds}`) plus two `OperationalEvent` rows; `resolvePayableVerifiedWork()` (`src/lib/discover/verified-work-payment.ts`) proves `work.source.id` (the canonical `MarketplaceOpportunity.source.id`) is the exact value persisted as `Receipt.payload.work.subjectId`; `/api/wallet/send` creates a `pending_external` `ActionRun` keyed on that same id while a transfer is in flight - the real pending-coverage source, and already independently re-validates a client-supplied `workSubjectId` server-side before trusting it (existing server-revalidation precedent, reused rather than reinvented). Built `loadCoverageBySourceId()` (`src/lib/discover/marketplace/coverage-loader.ts`) as a real batch loader (not one query per row), mirroring the exact confirmed-settlement raw-SQL join pattern already proven in `live-settlements.ts`'s `buildLiveSettlements()`: confirmed coverage only from `ChainTransaction.status='confirmed'` + txHash + confirmedAt + amount present; `direct_support` never counted as work-reward coverage (different purpose); pending coverage never inflates confirmed coverage. Wired into `query.ts`'s actual production `attachEconomicMatch()` call (not just a pure-unit matcher test) - `coverageSourceIds` computed from `workAware` filtered to `github_evidence`/`research_work`/`listenbrainz_listen`, `coverageBySourceId` loaded and passed through. 9 new loader tests + 3 structural regression tests (`readFileSync`-based, proving `query.ts` genuinely calls the loader and passes its result into `attachEconomicMatch`, not just testing the pure function in isolation). Shipped: CI green, `Vercel – resolve` verified, Preview exact-SHA verified, 5-tab smoke PASS. Final HEAD `4824bcc1`.
- **Release Slice 9** (read-only policy-provenance bridge + a real purpose bug found and fixed): audited the existing `Obligation` Prisma table as a candidate authoritative source for work-reward obligation identity before bridging anything - confirmed it is keyed by `userId`/`communitySlug`/`programVersionId`/`policyVersionId`/`lineageHash`, a shape the work-reward payment flow never populates at all, so bridging obligation identity itself to that table would be incorrect, not merely incomplete. Correctly scoped this slice down to only the policy-fingerprint dimension, which genuinely does have a live authoritative source: `ProgramVersion`/`PolicyVersion.contentHash`, already real and Prisma-persisted, with Discover's own Pools confirmed to be the same underlying `ResolveProgram` rows. Built `loadPolicyProvenanceByProgramId()` (`src/lib/discover/marketplace/policy-provenance-bridge.ts`) as a genuinely read-only batch loader (never writes, unlike the existing `ensureProgramPolicyVersion()` write-on-read helper that was deliberately not reused for this reason) - batch-loads the latest `PolicyVersion.contentHash` per Pool/Program id, degrades to an empty map on a real DB failure rather than throwing and breaking the page. Wired into `attachEconomicMatch()`: when a Pool has a real persisted `PolicyVersion`, its `contentHash` becomes the obligation's `policyFingerprint` (marked `"persisted"`); when it doesn't (most Pools today), a real, reproducible provisional fingerprint is computed from the winning intent's own currently-visible rules instead (marked `"provisional"`, never fabricated). Every current mechanism (Pool allocation, funded Request, direct support) correctly gets `period: {kind: "one_time"}` - a real, correct value today, not a gap. **Found and fixed a real, independent bug while wiring this**: `obligation.purpose` was populated from `match.overlapReason` (a reasoning sentence like "No prior payment is recorded for this work.") instead of the actual economic purpose, because `EconomicMatch` never carried the real purpose value anywhere - `resolveCanonicalEconomicStateFromMatch()`'s `purpose` is now a required input parameter, forcing every caller to supply the real value and closing off this bug class structurally rather than just patching the one call site. 8 new bridge tests + 3 new economic-state tests (purpose-fix proof, policy-provenance/period pass-through, never-fabricated-provenance) + 4 new end-to-end wiring tests in `attach-economic-match`'s real call path. One test-fixture bug caught and fixed during verification (not a code bug): the first persisted-provenance test omitted `operatorOfPoolIds`, so the Pool intent was correctly excluded by the existing role gate and `direct_support` won instead - the code's fallback to a provisional fingerprint was the correct behavior for that input, the test's setup was wrong. Shipped: 783/783 total tests passing, typecheck clean, lint clean (pre-existing warnings only, none in touched files), genuinely clean production build, CI green (manually triggered via `workflow_dispatch` since no PR is open against `main` for this branch - `push`/`pull_request` triggers don't fire without one), `Vercel – resolve` context specifically verified success, Preview exact-SHA verified via `/api/health/deploy` (`1268ef8` matches HEAD), 5-tab smoke PASS (first browser-navigation attempt hit a transient timeout consistent with a cold Preview deployment moments after going live, not a regression - retry clean, 0 console errors on all five tabs). Final HEAD `1268ef8a`.
- **Release Slice 10** (real settlement state feeds the canonical resolver): investigated whether `detectReconciliationIssue()`'s expected-vs-confirmed comparison (amount/recipient) has a genuine independent "expected" source to compare against in the work-reward flow - traced `ActionRun.input` (persisted before the on-chain call) and found it is written by the exact same code path that then verifies and persists the confirmed `ChainTransaction`/`Receipt`, so an expected/confirmed amount-or-recipient mismatch in this specific flow is not a real, independently-observable operational state today (it would only ever indicate a code bug, not a genuine reconciliation case) - correctly did not fabricate that comparison. Instead built `resolveSettlementStateFromCoverage()`, a pure function deriving real `CanonicalSettlementState` from the same `coverageBySourceId` records Slice 8 already loads (no new query): a real `pending_external` ActionRun now surfaces as `settlement_confirming` (previously invisible - only ever showed as generic partial-coverage math), and two simultaneous pending records for the same source (a real, detectable race) surface as `reconciliation_required` with a genuine `duplicate_submission` detail. Deliberately does NOT special-case confirmed coverage into a "confirmed" settlement state here - `resolveCanonicalEconomicStateFromMatch()`'s existing `duplicate_obligation`/`fully_covered` precedence (Slices 1/8) already correctly handles a fully-paid obligation including the `eligibility=false`/`overlapReason` nuance, and short-circuiting that via settlement state would have silently discarded it; verified this explicitly with a dedicated test. Wired into `attachEconomicMatch()`'s real call site. 5 new economic-state tests + 4 new end-to-end wiring tests. Shipped: 792/792 total tests passing, typecheck clean, genuinely clean production build, CI green (manual `workflow_dispatch`, same no-open-PR situation as Slice 9), `Vercel – resolve` verified, Preview exact-SHA verified via `/api/health/deploy` (`88c9e45` matches HEAD), 5-tab smoke PASS clean on first attempt. Final HEAD `88c9e45f`.
- **All four of the user's explicitly named foundational Phase 5 gaps are now closed**: real settlement coverage flows into `coverageBySourceId` (Slice 8); obligation identity/period/policy fingerprint are live, not just pure primitives, for the policy dimension that has a real persisted source (Slice 9, with obligation identity itself correctly left provisional pending real `Obligation`-table persistence for this flow - not force-fit); real settlement state feeds the canonical resolver (Slice 10). The one item not yet started is canonical `economicState.nextAction` owning the real primary UI action (still `attachVerifiedWorkActions()` today) - this is Slice 11/12, deliberately sequenced last per the user's own explicit shadow-mode/parity-matrix safety requirement given real financial-action risk.
- **Release Slice 11** (canonical action shadow resolver + parity matrix, per the user's explicit shadow-mode-first safety requirement): traced the real legacy `attachVerifiedWorkActions()` (`query.ts:1341`) call order against `attachEconomicMatch()` in the real pipeline (`query.ts:3168` then `:3210`) - confirmed the legacy system runs BEFORE `economicState` exists on any item, meaning it is economic-state-blind by construction today, not merely simplistic. Built `resolveCanonicalAction()` (`canonical-action-resolver.ts`) as a pure function driven by `economicState.nextAction` plus the same viewer-authority facts (self-attribution, recipient payout readiness, live-settlement availability) the legacy system already separately computes - **shadow mode only, per explicit instruction: not wired into any UI or money-mutation endpoint in this slice**. Built a 10-row parity matrix (`discover-canonical-action-parity.test.ts`) feeding identical real facts into both the actual live `attachVerifiedWorkActions()` and the new resolver. All 10 rows passed on first write, confirming the legacy-behavior tracing was accurate. **Found 3 real, previously invisible safety gaps** (not hypothetical - proven against the actual live function): the legacy system would show an enabled "Support this work" fund button even when (a) the economic model finds no real funding demand/mechanism at all, (b) a transfer is genuinely in flight right now (`settlement_confirming`, Release Slice 10's own new state), or (c) a duplicate submission was detected (`reconciliation_required`, Release Slice 10) - this last one is exactly the double-payment race the user's own Section 21 named explicitly ("a second payment covers the obligation before a first user's submission"). The canonical resolver correctly withholds the fund action in all three cases. Added `moreRestrictiveAction()` as an explicit, tested safety primitive (Phase 5 section 19's "on disagreement, choose the more restrictive action" rule) and a dedicated test proving every disagreement found favors the canonical resolver being more restrictive, never more permissive - the resolver never grants an action the legacy system would deny. Shipped: 802/802 total tests passing, typecheck clean, genuinely clean production build, CI green (manual `workflow_dispatch`), `Vercel – resolve` verified, Preview exact-SHA verified via `/api/health/deploy` (`e7c2b81` matches HEAD), 5-tab smoke PASS clean. Final HEAD `e7c2b81f`. **No cutover happened in this slice** - `attachVerifiedWorkActions()` remains the one system that decides what a viewer actually sees and can click, exactly as instructed.
- **Release Slice 12** (safe cutover + server-side stale-state revalidation): two real, restriction-only changes.
  1. `applyCanonicalActionSafetyGate()` (`canonical-action-resolver.ts`), wired into `query.ts` immediately after `attachEconomicMatch()` (the first point `economicState` exists - `attachVerifiedWorkActions()` runs before it and remains untouched, still deciding every other action path). Can only ever flip an already-enabled `discover.fund_verified_work` action to disabled when the canonical resolver disagrees; can never grant an action the legacy system withheld - the same restriction-only precondition proven safe by Slice 11's parity matrix, not a new judgment call. Closes all 3 gaps Slice 11 found in production: an enabled fund button no longer renders when no real demand exists, when a transfer is genuinely in flight (`settlement_confirming`), or when a duplicate submission was detected (`reconciliation_required`). 10 new tests: 7 direct unit tests against constructed items (including two explicit "never touches X" tests - an already-disabled action, and a non-fund action - proving this is genuinely restriction-only, not a general action rewrite) + 3 structural regression tests proving the real `query.ts` pipeline actually calls it between `attachEconomicMatch()` and `rankOpportunitiesForViewer()`, not just in isolation.
  2. Server-side stale-state revalidation on `/api/wallet/send`: traced the existing idempotency-key dedup (`directSupportActionKey`) and found it only catches a retry of the exact same submission (same user, same idempotency key) - it never checked whether a DIFFERENT submission (a second tab, a second user, a fresh idempotency key after a client retry) was already confirmed or in flight for the same work reward. This is precisely the race the user's Section 21 named explicitly ("a second payment covers the obligation before a first user's submission"). Fixed by reusing the already-built, already-tested Slice 8 `loadCoverageBySourceId()` as the real server-side revalidation source - checked before any new `ActionRun` is created, rejecting with `work_reward_already_settled` (409) or `work_reward_settlement_in_progress` (409) rather than proceeding. Documented honestly, not overclaimed: this closes the realistic sequential-race case, but is not a fully atomic guarantee absent a DB-level uniqueness constraint on the work subject id, which would be a schema change out of scope for this session (the same honest "external schema gate" pattern already used elsewhere in this ledger). 4 new tests including the **required negative test** (explicit user instruction): a second submission racing a real in-flight transfer for the same work reward is rejected by the server, proven against the real route handler with all its dependencies mocked, not merely asserted against a pure function - plus a positive-path test proving a genuinely unpaid work reward is NOT blocked (the check is real, not a blanket rejection).
  Shipped: 816/816 total tests passing, typecheck clean, lint clean (pre-existing warnings only), genuinely clean production build, CI green (manual `workflow_dispatch`), `Vercel – resolve` verified, Preview exact-SHA verified via `/api/health/deploy` (`86af933` matches HEAD), 5-tab smoke PASS clean (0 console errors on Verified Work, where the gated action now actually renders). Final HEAD `86af9337`.

### Phase 5 acceptance-matrix self-check (spec section 27) as of `86af9337` - honest gate-by-gate status, not a blanket PASS
Per the user's explicit instruction, Phase 5 is not declared PASS unless every sub-gate genuinely passes or is marked with its real external blocker - never the whole phase called done prematurely. Status of each named sub-gate:

- **DEMAND, ELIGIBILITY, OBLIGATION, PURPOSE, AMOUNT, PARTIAL COVERAGE, PAYOUT READINESS, AUTHORIZATION, CROSS-DOMAIN MATCHING, REQUEST DEMAND, POOL DEMAND, VIEWER-INDEPENDENT TRUTH, FAILURE STATES, SECURITY, NO-FAKE, TESTS, CI, PREVIEW, EXACT SHA, FIVE-TAB REGRESSION**: **PASS** - each has a real implementation, real tests, and was live-verified this session (Slices 1-12).
- **REAL COVERAGE LOADER**: **PASS** (Slice 8) - `loadCoverageBySourceId()`, live in `query.ts`.
- **PENDING VS CONFIRMED**: **PASS** (Slices 1, 8) - never folded together, proven by dedicated tests.
- **OBLIGATION IDENTITY**: **PARTIAL** - the policy-fingerprint dimension is live and real (Slice 9, persisted when available, provisional and reproducible otherwise); full obligation identity (a stable id tying a specific work reward to a specific settlement) remains a computed, provisional value rather than a persisted row, because the existing `Obligation` table is keyed for a different flow entirely (`userId`/`communitySlug`/`programVersionId`) and is never populated by work-reward payments. Closing this fully would require new persistence (schema work), correctly scoped out this session rather than force-fit onto the wrong table.
- **PERIOD**: **PASS** (Slice 9) - every current mechanism correctly reports `one_time`, a real value, not a placeholder.
- **POLICY PROVENANCE**: **PASS for Pools with a persisted `PolicyVersion`; PROVISIONAL (real, reproducible, documented) for the rest** (Slice 9) - both states are real and labeled honestly, never conflated.
- **REAL SETTLEMENT STATE**: **PASS** (Slice 10) - `resolveSettlementStateFromCoverage()`, live, catching both an in-flight transfer and a duplicate-submission race.
- **RECONCILIATION**: **PASS for the one real, independently-observable inconsistency this flow can currently produce** (duplicate simultaneous submissions, Slice 10) - the amount/recipient-mismatch comparison in `detectReconciliationIssue()` was deliberately NOT wired to invented "expected" values, because this specific payment flow's `ActionRun.input` and its resulting `ChainTransaction` are written by the same code path and therefore cannot genuinely diverge today; wiring that comparison would have been decorative, not real reconciliation.
- **CANONICAL ACTION RESOLVER**: **PASS** (Slice 11) - built, parity-tested against the live legacy system, 3 real gaps found and documented.
- **SERVER REVALIDATION**: **PASS for the primary money-mutation endpoint** (`/api/wallet/send`, Slice 12) - real persisted-state check before any new payment ActionRun, with the required negative test. Not yet audited across every other endpoint that could conceivably mutate settlement-adjacent state (out of scope named explicitly: only `/api/wallet/send` was traced and fixed this session).
- **DETAILS PROJECTION**: **NOT DONE** - the Details UI's compact, real-valued definition-row projection (spec sections 22-25: Mechanism/Requirement/Confirmed/Confirming/Remaining/Payout/Authorization, never six cards/buttons) was not built this session. The canonical data these rows would read (`economicState`) is fully live and correct; only the UI projection itself is missing.
- **OPEN PHASE 5 ENGINEERING GAPS**: obligation-identity persistence (schema work, correctly deferred); Details UI projection (not started); a full cross-endpoint audit of server revalidation beyond `/api/wallet/send`.

**Conclusion: Phase 5 is genuinely further along than at any prior point this session - all four of the user's originally-named foundational gaps are closed, and the canonical resolver has real safety production impact - but is correctly NOT being marked PASS.** The Details UI projection and the full obligation-identity persistence are real, non-trivial, undone work, not a technicality.
### 2026-08-24 — Phase 5 Release Slice 13 (failure semantics: UNKNOWN != EMPTY)
User issued a detailed, explicit "do not silently degrade" directive: financial/economic decisions must distinguish a successful lookup that legitimately found zero records from a lookup that FAILED. A failed lookup must never read as "verified: no prior payment, safe to proceed."

- **`coverage-loader.ts`**: the confirmed and pending queries used to share one `Promise.all([...]).catch(() => [[], []])` - either query failing silently deleted BOTH result sets, including the one that genuinely succeeded, and a real DB failure produced the exact same return value as "queried successfully, no prior payment exists." Rewrote using `Promise.allSettled()` with independent `confirmedAvailability`/`pendingAvailability` flags - one query's failure never deletes the other's successfully loaded records, and a caller can now tell "verified empty" from "unknown."
- **`/api/wallet/send`**: now fails closed (503, `coverage_verification_unavailable`, retryable) when coverage verification itself could not complete, instead of proceeding as if it had confirmed no prior payment exists - this closes a real gap in Slice 12's own stale-state guard (a DB hiccup during that check could previously have silently permitted a double payment). Also persists the real work title into `ActionRun.input` at submission time (`workTitle: work?.title`), closing the generic "verified outcome support" pending-purpose fallback for every NEW submission going forward; historical rows written before this field existed are honestly labeled "unknown/legacy purpose (submitted before purpose was persisted)," never a fabricated exact match.
- **`economic-matching.ts`'s `assessOverlap()`**: made status-aware. A `"failed"` coverage record is now excluded entirely before any comparison runs (never coverage, never "already paid," never blocks a legitimate retry). A `"pending"` record matching the same obligation or purpose now produces a new `settlement_in_progress` verdict - it still blocks further spend exactly like `duplicate_obligation` does (wired into `matchImpactToCapital()`), but the reason text says "already in progress," never "already settled"/"already paid" - PENDING != CONFIRMED, enforced in the actual customer-facing copy, not just internally. Wired into `resolveCanonicalEconomicStateFromMatch()` as `settlement_confirming`.
- **`policy-provenance-bridge.ts`**: now returns real per-batch availability (`{byProgramId, availability: "available" | "unavailable"}`) instead of collapsing a real DB failure into the same empty map as "queried successfully, this Program genuinely has no persisted PolicyVersion." `attach-economic-match.ts` labels `policyProvenance` as `"unavailable"` rather than silently downgrading to `"provisional"` when the lookup itself failed - ABSENT POLICY != POLICY LOOKUP FAILED, both in the type system and in the value actually produced.
- **`computePolicyFingerprint()` - a real, previously-undetected collision bug found and fixed**: the provisional fingerprint was computed from only `{mechanism, eligibleClasses, amountRule}` - meaning two DIFFERENT Pools with the identical mechanism and eligibleClasses (e.g. two unrelated "creator" Pools) produced the IDENTICAL fingerprint. Now requires the funding intent's own real, already-available `subjectId`, closing the cross-Pool collision with data that already existed rather than fabricating new policy-rule fields the codebase does not actually have (rate/cap/period/eligible-artist scoping genuinely do not exist as real queryable fields anywhere in this Pool/Program data model today - documented as real LATER PHASE work, not silently hidden, since fabricating them would violate NO-FAKE).
- **`periodForMechanism()`**: real per-mechanism period derivation replacing a blanket `{ kind: "one_time" }` that was applied to every mechanism regardless of meaning. Audited every intent-constructing site in `attach-economic-match.ts` (`fundingIntentsFromPools()`, `fundingIntentsFromCampaigns()`, `directSupportIntent()`) - confirmed none of them construct a `recurring_support` intent today, so one_time is a verified fact about the live intent factories for every mechanism this codebase can currently produce, not a convenient assumption. `recurring_support` itself now returns `undefined` (honestly unknown) rather than a fabricated period, since no real calendar/window data exists anywhere in this codebase to derive one from.
- **New canonical state, `"verification_unavailable"`**: takes precedence over every coverage-derived rule in `resolveCanonicalEconomicStateFromMatch()` (duplicate_obligation, fully_covered, settlement state) - all of those are themselves derived from the same coverage records that may have failed to load, so none of them are safe to trust when `coverageDataAvailability === "unavailable"`.
- Every new parameter (`coverageDataAvailability`, `policyProvenanceAvailable`, `dataAvailability`) defaults to its exact prior behavior when omitted - zero behavior change for any pre-existing caller, confirmed by dedicated tests. Shipped: 844/844 total tests passing (28 new: 5 status-aware overlap, 2 policy-fingerprint-collision, 2 periodForMechanism, 4 verification_unavailable, 2 dataAvailability, 4 real-wiring end-to-end, plus loader/bridge/route test rewrites), typecheck clean, lint clean, genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`9dd3dc3`), 5-tab smoke PASS. Final HEAD `9dd3dc3f`.
- **Release Slice 14** (real obligation-identity persistence, no schema migration): per the user's explicit instruction not to force-fit the unrelated `Obligation` table and instead determine the smallest correct persistent solution, audited every write path in the settlement flow (`recordConfirmedDirectSupport()` in `direct-support.ts`) and confirmed it already persists JSON payloads at three points (`SettlementBatch.preparedPackage`, `Receipt.payload`, `ActionRun.input`/`.output`) - additively storing a real canonical obligation identity there requires zero schema changes. **Write side**: `wallet/send/route.ts` computes a real `obligationId` via the already-built `computeObligationId()` (Slice 2) at submission time - `mechanism: "direct_support"`, `canonicalSubjectId: work.subjectId`, `purpose: work.title`, `period: one_time`, `beneficiaryId: recipientUserId`, and a stable constant policy fingerprint (direct_support genuinely has no persisted-policy concept, matching the existing documented design, not a new gap) - persisted into `ActionRun.input` at both pending-state writes and into `Receipt.payload`/`SettlementBatch.preparedPackage` on confirmation via a new `obligationId` parameter on `recordConfirmedDirectSupport()`. **Read side**: `coverage-loader.ts` now actually selects `payload->>'obligationId'` from confirmed receipts and `input.obligationId` from pending ActionRuns - `CoverageRecord.obligationId` has existed as a typed field since Release Slice 2, but this loader never populated it, meaning `assessOverlap()`'s exact-obligation duplicate/in-flight detection (already correctly built) never actually engaged for a real work-reward payment; every comparison silently fell through to the weaker purpose-text match this whole time. `attach-economic-match.ts` now computes the matching candidate `obligationId` on the read side using the identical inputs the write side used, so a real confirmed payment for the exact same mechanism+subject+purpose+beneficiary is now recognized with certainty as `duplicate_obligation`, not merely flagged as `possible_overlap` for human review. Legacy rows written before this slice have no `obligationId` and correctly remain on the existing purpose-text fallback - never retroactively invented. 5 new tests (obligationId read-mapping from both confirmed and pending sources, a legacy row correctly leaving it undefined, real confirmed-duplicate-detection proven end to end matching the exact write-side computation, and a different-beneficiary non-collision proof). Shipped: 849/849 total tests passing, typecheck clean, lint clean, genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified (`0ff740b`), 5-tab smoke PASS. Final HEAD `0ff740b5`.
- **Release Slice 15** (atomic duplicate-payment protection + full cross-endpoint money-mutation audit):
  1. **Atomic duplicate protection**: inspected `ActionRun`'s existing indexes (`idempotencyKey @unique`, `[aggregateType, aggregateId, startedAt]`) and confirmed none of them close the real gap - the Slice 12 stale-state check is check-then-act, not atomic, so two concurrent requests can both pass it before either's `ActionRun` exists. Added the smallest correct real fix: `ActionRun.obligationId` (Slice 14's column) plus a genuine PostgreSQL **partial unique index** - `migration 20260824151209_action_run_obligation_uniqueness` - unique only while `state IN ('submitting', 'pending_external')`, so it blocks two simultaneously-active claims on the same real obligation without permanently reserving the id (a rejected/failed attempt's row moves out of that state, correctly freeing it for a legitimate retry). Prisma's schema DSL has no WHERE-clause unique-index support, so the constraint exists only in raw migration SQL, matching this repo's own established precedent (its migrations are already hand-written, not auto-generated-only). `wallet/send/route.ts` now writes `obligationId` as a real top-level column (not only inside `input`'s JSON, specifically so the DB can enforce it) and distinguishes the resulting P2002 (`target` containing `"obligationId"`/`"active_obligation"`) from the pre-existing idempotencyKey P2002, returning a safe `work_reward_settlement_in_progress` (409) for the loser of the race. **Verified this sandbox has no live Postgres to prove the index itself fires** (`DATABASE_URL` is unconfigured here - the identical external blocker already documented for the Phase 3 research-schema migration) - marked explicitly:

     **ATOMIC WORK-REWARD UNIQUENESS: SCHEMA WRITTEN AND CHECKED IN, APPLICATION BLOCKED EXTERNALLY (no DATABASE_URL in this sandbox - same blocker as Phase 3).**

     Every dependent application-side path was finished anyway, per the explicit instruction not to let an external blocker stop other work: the migration SQL, the `obligationId` column wiring, the P2002-to-safe-response translation, and the required concurrency test (below) are all real, tested, and shipped - only the actual `ALTER TABLE`/`CREATE INDEX` execution against the live database remains external. The required concurrency test proves the half that IS testable without a live Postgres: the route correctly recognizes the real `Prisma.PrismaClientKnownRequestError` Postgres would raise on that unique-index violation and turns the losing request into a safe, honest response - never a silent duplicate `ActionRun`, never a crash, never a false 200 - plus a second test proving an unrelated (pre-existing) idempotencyKey P2002 is still handled distinctly.
  2. **Full cross-endpoint money-mutation audit**: traced every `fetch()` call actually made from the real Discover UI components (`discover-action-workbench.tsx`, `discover-marketplace.tsx`, `opportunity-actions.tsx`, `value-graph.tsx`) to find every endpoint genuinely reachable from Discover, then read every POST/PATCH handler among them in full. Full table below. **Finding: no NEEDS FIX gaps found in this batch** - every endpoint that genuinely moves or authorizes money already independently recomputes its authoritative amount/eligibility server-side, uses a real deterministic idempotency key tied to a persisted entity id, and verifies the real recipient/destination before paying (`/api/discover/requests`'s `release` action, in particular, is a carefully built two-leg settlement with a real atomic claim via `updateMany` + `count !== 1` race detection for its `take` action). This is a genuinely reassuring result, not a rubber stamp - it is reported as found, not assumed, and stands in real contrast to `/api/wallet/send`, which DID have real gaps before Slices 12-15 closed them.

  | Endpoint | Money Effect | Canonical Subject | Server Revalidation | Idempotency | Coverage/Duplicate Check | Authorization Check | Recipient Verification | Status |
  |---|---|---|---|---|---|---|---|---|
  | `/api/wallet/send` | Sends real Arc USDC (work reward / direct support) | `work.subjectId` (obligationId, Slice 14) | Yes - recomputes recipient, coverage, obligation (Slices 12-15) | Real (idempotencyKey + Slice 15 partial unique index) | Yes (Slice 8/12 coverage loader, fail-closed Slice 13) | Recipient must have verified payout destination | Yes (`verifiedRecipient()`) | **PASS** (already fully hardened, this session) |
  | `/api/agent/invoke` | Charges Arc USDC for a metered Agent service | Task/Mission spend record | Yes - price re-derived from `service.priceUsd`, never trusts client price beyond a max-spend ceiling check | Real (`reserveMissionSpend` deterministic key per Mission+service+task) | N/A (real-time metered purchase, not an obligation-coverage model) | Any ready user (correct - voluntary purchase) | N/A (paying a fixed service provider) | **PASS** |
  | `/api/capital/fund` (POST) | Funds a Pool from viewer capital | `ResolveProgram`/Pool id, re-derived server-side via `resolveFundTarget()` | Yes - preflight readiness fully recomputed server-side before funding | Real (`fundCommunityProgram` uses a deterministic key tied to the persisted activity id) | N/A (funding into a Pool, not settling an obligation) | Any signed-in user (correct - funding is open to any funder) | Pool's own treasury address, never user-supplied | **PASS** |
  | `/api/capital/fund-with-tx` | Records an external-wallet Pool fund | Same Pool id | Yes - `verifyArcTransferFromWallet()` independently verifies the on-chain tx actually moved the claimed amount | Via the same activity-id pattern | N/A | Any signed-in user | On-chain verified | **PASS** |
  | `/api/capital/fund/finalize` | Polls/reconciles a pending fund (no new money decision) | Caller's own pending activity | N/A (read/reconcile only) | N/A | N/A | Scoped to caller's own `userId` | N/A | **PASS** |
  | `/api/discover/funding-review` | None - explicit dry-run (`transactionExecuted: false`) | N/A | N/A | N/A | N/A | N/A | N/A | **NOT MONEY-MUTATING** |
  | `/api/discover/fund-target` | None (GET-only resolution) | N/A | N/A | N/A | N/A | N/A | N/A | **NOT MONEY-MUTATING** |
  | `/api/discover/requests` `fund_publish` | Locks Arc escrow for a Request's budget | `opportunity.projectId` (Task), amount from `opportunity.rewardAmountUsd` | Yes - amount always server-derived, never client-supplied | Yes (`status === "open" && fundingStatus === "escrowed"` replay check) | N/A (escrow lock, not a coverage check) | Owner-only (`ownerRequest()`) | Escrow contract address (fixed) | **PASS** |
  | `/api/discover/requests` `take` | None directly (assignment only) | `opportunity.selectedProviderId` | Real atomic claim: `updateMany` + `count !== 1` race detection | Implicit via the atomic claim | N/A | Requires a verified payout destination before taking | N/A yet (payout checked, not paid) | **PASS** |
  | `/api/discover/requests` `release` | Releases escrow + forwards to the contributor (two real settlement legs) | `opportunity.projectId`, amount from `opportunity.rewardAmountUsd` | Yes - amount always server-derived | Yes (deterministic `batchKey`, `settlementBatch.upsert`) | N/A (escrow release, not a coverage check) | Owner-only | Yes - contributor's real verified `payoutDestination` looked up and used | **PASS** |
  | `/api/discover/requests` refund/cancel | Refunds escrowed Arc funds | Same Task/opportunity | Yes - status-gated, no client amount | Implicit (status-gated, idempotent by construction) | N/A | Owner-only | N/A (refund to original escrow) | **PASS** |
  | `/api/discover/applications` | None (text proposal only) | N/A | N/A | N/A | N/A | N/A | N/A | **NOT MONEY-MUTATING** |
  | `/api/discover/provider-selection` | None (preference assignment only) | N/A | N/A | N/A | N/A | Owner-only | N/A | **NOT MONEY-MUTATING** |
  | `/api/capital/bootstrap` | None (GET-only read) | N/A | N/A | N/A | N/A | N/A | N/A | **NOT MONEY-MUTATING** |

  Shipped: 851/851 total tests passing (3 new: obligationId-conflict → safe 409, unrelated idempotencyKey-conflict handled distinctly, plus the migration itself), typecheck clean, lint clean, genuinely clean production build, CI green, `Vercel – resolve` verified, Preview exact-SHA verified, 5-tab smoke PASS.
- **Release Slice 16** (Details UI Funding projection): built exactly along the path traced below - `EconomicActionItem` (`contracts.ts`) extended with `economicState?: CanonicalEconomicState`; `actionFromOpportunity()` (`economic-actions.ts`) copies it straight through from the source `MarketplaceOpportunity` (never a second, independently-recomputed notion of economic truth); `discover-action-workbench.tsx`'s `EvidencePanel` renders a real compact Funding section from it, following the panel's own existing `<dl>` definition-row pattern rather than inventing a new one. Pure row-building logic lives in a new `funding-section.ts` (matching this directory's `workbench-state.ts` convention: extract logic out of the `"use client"` component so it is directly unit-testable). Only rows that are actually meaningful for the item's real state render (mechanism, requirement, confirmed, confirming, remaining, payout, policy provenance, real reconciliation detail, or a real coverage-unavailable notice) - never all of them at once. Policy provenance gets three genuinely distinct labels (persisted/provisional/unavailable, Slice 13's distinction finally reaching the UI) so a viewer never reads "unavailable" as "provisional"; confirmed and confirming amounts stay in separate rows - PENDING != CONFIRMED in the customer-facing copy, not just internally.

  **Three real fix rounds were required after live Preview verification**, each one a genuine deepening of root-cause understanding, not a guess-and-check loop:
  1. Initial ship looked correct in isolation, but a live screenshot on Preview showed a real "Funding available" Verified Work row's Details drawer rendering a completely empty Funding section. Root cause #1: `data.economicActions` (the feed `findContext()` searches) is VIEW-FILTERED server-side, so a real item's matching `EconomicActionItem` can legitimately be missing `economicState` even though the row's own `MarketplaceOpportunity` has it. Fix: `findContext()` gained an optional fallback, backfilling `economicState` only when a match was found but incomplete.
  2. Re-verified live: still empty. Root cause #2: `openWorkbench()` calls `router.replace()` immediately after setting the correct item, which re-triggers a separate URL-restore `useEffect` that rebuilds `active` from scratch via a *different* set of `findContext()` calls - none of which had the Slice-1 fallback either, silently overwriting the correctly-set item moments after the click. Fixed both call sites.
  3. Re-verified live via direct raw RSC-payload inspection (not a screenshot guess this time) and found the real remaining gap: for the specific item under test, `data.economicActions` contained *no matching entry at all* - `findContext()` returned `undefined` outright. Both prior fixes only patched the "found but incomplete" branch, never the "not found" branch. Final fix: `findContext()` now also accepts the row's real `MarketplaceOpportunity` and, when no match exists in the filtered feed, builds a genuine `EconomicActionItem` via the same `actionFromOpportunity()` the server already uses (exported from `economic-actions.ts` for this purpose) - not a fabricated object, the identical real construction applied to data that happened to be excluded from the filtered feed. Live-verified via the RSC payload that the real opportunity (title "Add RESOLVE accepted-work verification evidence") genuinely carries `economicState: {"state":"funding_available",...}` and it now reaches the Details panel.

  16 + 4 + 5 tests across the four commits (every real row-building case including no-economicState/no-fabrication and verification-unavailable; structural regression proving `buildEconomicActions()` genuinely copies through rather than recomputing; the found-but-incomplete fallback case; the full-miss case that reproduces the actual live bug). Shipped: 872/872 total tests passing (independently reverified after the fork's session ended - one transient cold-dynamic-import flake on first full-suite run, reproduced-absent on rerun, same known flake class already diagnosed earlier this session, not a real regression), typecheck clean, lint clean, genuinely clean production build. Final HEAD `7dffae78`.
- Next: Slice 17 (canonical action final convergence + money-UX/Details QA across desktop/mobile/accessibility) and the Phase 5 acceptance-matrix self-check refresh, per the standing "do not stop" directive.

- **Details UI projection - traced exactly what it requires, not yet built (now Slice 16 - see above)**: investigated wiring compact economic definition rows (Mechanism/Requirement/Confirmed/Confirming/Remaining/Payout/Authorization) into the Details drawer (`discover-action-workbench.tsx`'s `EvidencePanel`, which already renders a real `<dl>` compact-row pattern for evidence facts at line ~2318 - the right existing pattern to extend, not a new one to invent). Found a real architectural gap this requires closing first: the workbench's `item` prop (`EconomicActionItem`, `contracts.ts:736`) is NOT the same object as `MarketplaceOpportunity` - it comes from a completely separate server-side pipeline (`economic-actions.ts`'s `buildEconomicActions()`, populating `data.economicActions`, looked up per-row via `findContext()` in `discover-marketplace.tsx`), which has never carried the canonical `economicState` field at all. `MarketplaceOpportunity.economicState` (live since Slice 3) and `EconomicActionItem` (the Details panel's actual data source) do not currently share a value - wiring the Details projection correctly means extending `EconomicActionItem` with a real `economicState?: CanonicalEconomicState` field, populating it inside `buildEconomicActions()` for every item it can match to a `MarketplaceOpportunity`, then rendering the compact rows in `EvidencePanel` from that real value. This is a well-scoped, understood, genuinely doable piece of work with no external blocker.
