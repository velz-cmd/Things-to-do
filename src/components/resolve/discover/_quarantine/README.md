# Quarantined Discover components

These files are **not imported** by `DiscoverSurface` or the Discover tab. Kept for reference or one-off imports elsewhere.

| File | Reason |
|------|--------|
| `discover-ecosystem-rail.tsx` | Superseded by nav-only footer + Program tab |
| `discover-graph-preview.tsx` | Superseded by `DiscoverValueBubblemap` |
| `discover-value-graph-panel.tsx` | Superseded by bubble node popover |

Do not wire these back into `/discover` without an explicit product decision.
